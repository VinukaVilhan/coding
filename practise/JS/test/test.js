var score = 60;
var message='';
 
switch(score) {
    //compring the boolean result
  case (score === 69):
    message += 'Not bad!';
    break;
  case (100): 
       message += 'Great, perfect score!';
}

console.log(message);